# Revenge Backup

Revenge Backup adds a settings page that compiles every installed Revenge plugin link plus its stored settings into a local backup. It supports manual backups, scheduled automatic backups, exporting the backup as a JSON file, and managing backed-up entries one by one.
